# get_next_line

This project reads the line of one file

## Installation

When you have Git cloned the repository, to compute the project, you need to givethe function one file and give the name in the main. With the bonus, you can do the same but with multiple files (max: 1024 for a program). The final step is to make the program.

## Use

To use this project, you can make it in the project and call it. To call, you need to do this:
- get_next_line(fd);

## Author

MILLET Mehdi